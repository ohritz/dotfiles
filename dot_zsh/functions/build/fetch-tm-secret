# Build tools function: Fetch TM secrets from S3
# Dependencies: aws, TM_LOCAL_DEV_PATH environment variable
# Purpose: Fetch secrets from S3 bucket
# Usage: fetch-tm-secret

fetch-tm-secret() {
  source "${TM_LOCAL_DEV_PATH}/common.sh"
  aws-tm
  aws s3 cp s3://$AWS_SECRET_PATH secrets.json
}
